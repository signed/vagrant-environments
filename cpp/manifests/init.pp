package { 'git':
  ensure => installed
}

package { 'build-essential':
  ensure => installed
}

package { 'libudev-dev':
  ensure => installed
}

package { 'libusb-1.0-0-dev':
  ensure => installed
}

package { 'libfox-1.6-dev':
  ensure => installed
}

package { 'autotools-dev':
  ensure => installed
}

package { 'autoconf':
  ensure => installed
}

package { 'libtool':
  ensure => installed
}
